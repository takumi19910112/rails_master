<main class="main">
  <div class="inner">
<div class="card">
 <div class="card__body">
<% @favorite_questions.each do |question|%>

    <%= "#{l question.created_at}  投稿" %>
     <%= link_to question.title, question_path(question.id),method: :get,class: :card__title %>
     <%= link_to "by "+ question.user.nickname, user_path(question.user.id),method: :get, class: :card__user %>

<% end %>
</div>
</div>
</div>
</div>