
<%= form_with model: [question, comment] , remote: true do |form| %>
  <div >
    <%= form.text_area :message ,class: :comment__form , placeholder:"マークダウンが使えます。コードブロックを使用する際は１行あけてください。"%>
  </div>
  <div class="actions">
    <%= form.submit "コメントをする" %>
  </div>
<% end %>
